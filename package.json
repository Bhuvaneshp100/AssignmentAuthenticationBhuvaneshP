{
  "name": "assignment",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "DependencyInstall": " npm i && npm install -D @playwright/test && npx playwright install && npm install monocart-reporter --save-dev",
    "test": "playwright test",
    "cucumber": "cucumber-js "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.45.2",
    "@types/node": "^20.14.11",
    "playwright": "^1.45.2"
  },
  "dependencies": {
    "@cucumber/cucumber": "^10.8.0",
    "monocart-reporter": "^2.6.1",
    "ts-node": "^10.9.2"
  }
}
